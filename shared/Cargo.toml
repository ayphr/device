[package]
name = "ayphr-protocol"
version = "0.1.0"
edition = "2021"

[features]
default = []
std = []
serde = ["dep:serde"]

[dependencies]
serde = { version = "1.0", default-features = false, features = ["alloc", "derive"], optional = true }
